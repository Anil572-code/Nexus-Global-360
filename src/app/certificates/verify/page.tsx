import { Suspense } from "react";
import CertificateVerificationQuery from "@/components/certificates/CertificateVerificationQuery";
export default function CertificateVerificationPage(){ return <Suspense fallback={null}><CertificateVerificationQuery/></Suspense>; }
